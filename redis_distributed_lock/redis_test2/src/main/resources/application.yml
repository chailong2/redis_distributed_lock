spring:
  redis:
    host: 127.0.0.1
    port: 6379
    #password:
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8081